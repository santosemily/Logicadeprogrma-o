#include <stdio.h>
#include <stdlib.h>

int main() {
  int i;
  int * pt;
  pt = (int *) malloc(5000*sizeof(int));
  for (i = 0; i < 5000; i++)
    {
      pt[i]=0;
      printf("%d", pt[i]);
    }
}
  
