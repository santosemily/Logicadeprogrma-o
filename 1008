#include <stdio.h>

int main() {
	int num, hora;
	float valorhora, salario;
	scanf("%d %d %f", &num, &hora, &valorhora);
	salario = hora * valorhora;
	printf("NUMBER = %d\n",num);
	printf("SALARY = U$ %.2f\n", salario);
    return 0;
}
